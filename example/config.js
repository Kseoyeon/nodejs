var config = {
    port: 3000,
    mysql: {
        host: "localhost",
        port: 3306,
        db: "testdb",
        username: "abc",
        password: "seoyeon",
    }
}

module.exports = config;
